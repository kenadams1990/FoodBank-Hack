{
  "name": "tidelift-foodbank-hack",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --workspace apps/web run dev",
    "build": "npm --workspace apps/web run build",
    "typecheck": "npm --workspace apps/web run check"
  },
  "workspaces": [
    "apps/agents",
    "apps/web",
    "packages/*"
  ],
  "overrides": {
    "cookie": "^0.7.2"
  }
}
