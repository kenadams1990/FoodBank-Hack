Cold-chain progress strip — mono labels joined by unicode connectors, the moving dot marks the active leg.

```jsx
<FlowStrip stages={['Vessel','Pickup','Processing','Food Bank']} activeIndex={1} />
```
