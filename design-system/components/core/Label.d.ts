/** ALL-CAPS mono section label — 13px/11px, 0.28em tracking. */
export interface LabelProps {
  children: React.ReactNode;
  /** Text color (default tertiary) */
  color?: string;
  style?: React.CSSProperties;
}
