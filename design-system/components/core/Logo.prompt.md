TideLift logo lockup — salmon mark on dark, slate ink on light; never recolor outside that set.

```jsx
<Logo variant="lockup" on="dark" size={28} />
<Logo variant="compact" on="dark" size={24} />  {/* nav bars, badges, tight chrome */}
```

Clear space = beacon-dot height. Never stretch, rotate, or add shadows.
