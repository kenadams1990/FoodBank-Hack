Display-size metric with mono unit label — lead every screen with a real number (lbs, meals, °C).

```jsx
<ImpactNumber value="38,400" unit="lbs recovered" />
<ImpactNumber value="1" unit="human approval" accent />
```

`accent` colors the figure salmon — reserve for one metric per view.
