TideLift action button — verb-first sentence-case copy; salmon primary is the single signal per screen.

```jsx
<Button variant="primary">Review recommendation</Button>
<Button variant="secondary">View audit trail</Button>
```

Variants: primary (salmon, dark ink text), secondary (transparent + hairline), danger (muted #C4432E). Sizes sm/md/lg. Hover lightens, press darkens — never shrinks.
