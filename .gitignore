node_modules
.env
.env.*
!.env.example

# Build output
.svelte-kit
/dist
build

# Package manager — pnpm is the workspace tool (see pnpm-workspace.yaml)
package-lock.json
yarn.lock

# OS / editor
.DS_Store
*.log

# ML model weights — served from the inference host, never committed
# (best.pt is ~357MB; GitHub rejects files > 100MB)
best.pt
*.pt
*.onnx
*.pth
*.weights
