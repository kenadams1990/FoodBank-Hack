node_modules
.env
.env.*
!.env.example

# Build output
.svelte-kit
/dist
build

# Package manager — pnpm is the workspace tool (see pnpm-workspace.yaml)
package-lock.json
yarn.lock

# OS / editor
.DS_Store
*.log

# Vision service — model weights and Python artifacts (never commit weights)
*.pt
*.onnx
services/vision/__pycache__/
services/vision/.venv/
services/vision/runs/
