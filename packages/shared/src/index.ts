// @tidelift/shared — single import surface for the Operations Intelligence Hub.
// Import types + mock data from '@tidelift/shared' (not deep relative paths).
export * from './types';
export * from './mockData';
